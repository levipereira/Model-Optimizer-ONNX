# Minimal example: default int8 quantization but never insert Q/DQ on MatMul (e.g. attention).
# Tune exclude_op_types / include_op_types for your latency vs accuracy trade-off.

version: 1
name: matmul_fp_exclude

defaults:
  autotune: default

modelopt:
  exclude_op_types:
    - MatMul
